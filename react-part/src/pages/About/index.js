import React from "react";
import commonStyle from "../style.module.css"

export default function About() {
    return ( <
        >
        <
        h1 className = { commonStyle.heading } > About < /h1> <
        div className = { commonStyle.container } > This is About < /div>

        <
        />
    )
}